{
  "name": "dashdown-asset-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Dev/release-only tooling that pre-builds Dashdown's self-hosted CSS and vendors its frontend dependencies into dashdown/static/vendor/. Users of the `dashdown` pip package never run this — the generated outputs are committed and shipped in the wheel.",
  "type": "module",
  "scripts": {
    "build": "node build-assets.mjs"
  },
  "devDependencies": {
    "@fontsource-variable/inter": "^5.0.0",
    "alpinejs": "^3.14.0",
    "daisyui": "^4.12.0",
    "echarts": "^5.5.0",
    "mermaid": "^11.15.0",
    "tailwindcss": "^3.4.0"
  }
}
